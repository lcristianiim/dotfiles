# Hermes: enable URL-copy mode alongside the default URL-launch mode.
# Ctrl+Shift+O = open URL (keyboard jump labels), Ctrl+Shift+Y = copy URL.
# URL mode is keyboard-only — there is no hover/click in foot by design.
[key-bindings]
show-urls-copy=Control+Shift+y